import 'package:flutter/material.dart';

class DeliveriesCard extends StatelessWidget {
  const DeliveriesCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}